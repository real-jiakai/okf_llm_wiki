# Change log

## 2026-07-04

* **Creation**: Initial bundle published as a public OKF demo. Added three model concepts (`models/`), the Messages API concept (`api/`), the prompt-caching guide (`guides/`), and the model-migration playbook (`playbooks/`).
